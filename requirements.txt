# Add runtime deps here. Keep this list light: llama-server does the heavy
# lifting (tokenization, chat templates, inference) and is built separately.
huggingface_hub>=1.0
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pillow>=10.0
numpy>=1.26
scipy>=1.11
httpx>=0.27
requests>=2.31
pyyaml>=6.0
